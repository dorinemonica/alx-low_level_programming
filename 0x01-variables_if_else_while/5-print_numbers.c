#include<stdio.h>

/**
 * main - main function entry
 *
 * printf displays the outcome
 *
 * Return: 0 (success)
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
